// Exercise #5: Concatenate the Alphabets
let companyName = "TechUp";

// Start coding here
let num = [1, 2, 3, 4, 5, 6];
for (const index in num && companyName) {
  console.log("Number " + num[index] + " character is " + companyName[index]);
};